
import React from 'react';

// This component is no longer used but kept as a safe export to prevent import errors.
const AIConsultant: React.FC = () => {
  return null;
};

export default AIConsultant;
